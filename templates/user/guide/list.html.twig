{% extends 'user/base.html.twig' %}

{% block main %}
    <div class="row">
        <div class="col-12">
            {% if paginator.totalItems > 0 %}
                <div class="row">
                    {% for guide in paginator.currentPageItems %}
                        {{ include('_fragment/_guide/_guide_link.html.twig', {guide: guide}) }}
                    {% endfor %}
                </div>

                <div class="row">
                    <div class="col-12">
                        {{ menu(pagination_menu) }}
                    </div>
                </div>
            {% else %}
                <div class="row">
                    <div class="col-12">
                        {{ 'search.error.no_results'|trans }}
                    </div>
                </div>
            {% endif %}
        </div>
    </div>
{% endblock %}