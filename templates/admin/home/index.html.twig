{% extends "admin/base.html.twig" %}

{% block main %}
    <div class="row mb-3">
        <div class="col-12">
            <div class="card card-outline card-primary">
                <div class="card-body">
                    {{ 'admin_dashboard.welcome'|trans({user: app.user.email}) }}
                </div>
            </div>
        </div>
    </div>

    {{ include('_fragment/_modal/_form_collection_removal.html.twig') }}
{% endblock %}