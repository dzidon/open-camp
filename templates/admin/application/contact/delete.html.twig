{% extends "admin/application/base_update.html.twig" %}

{% block application_update_content %}
    <div class="row">
        <div class="col-12">
            <div class="card card-outline card-primary">
                <div class="card-body">
                    <div class="row">
                        <div class="col-12 col-md-8 col-xl-5">
                            <p>
                                {{ 'crud.confirmation.application_contact_delete'|trans({name: application_contact.nameFull}) }}
                            </p>

                            {{ form(form_delete) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}